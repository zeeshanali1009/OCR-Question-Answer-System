# OCR-Question-Answer-System

An OCR‑powered question‑answering system that extracts text from documents or images and answers questions based on extracted content.

## 🚀 Overview

This project uses Optical Character Recognition (OCR) to extract text from images or scanned files, and then processes the extracted text to answer user questions using a QA model.

## 🧠 Features

- Extract text from images or PDFs using OCR  
- Preprocess text for downstream question‑answering  
- Answer questions based on extracted content  
- REST API / CLI interface (if applicable)  

## 📦 Requirements

Install dependencies:

```bash
pip install -r requirements.txt
